#!/bin/bash

# Create directory for the lab
mkdir -p ~/project/networking-lab

# Change working directory to the lab directory
cd ~/project/networking-lab

# Install required packages
sudo apt-get update
sudo apt-get install -y traceroute