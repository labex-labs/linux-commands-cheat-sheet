#!/bin/bash

# Create a directory for the lab
mkdir -p ~/project/disk-file-lab

# Change to the lab directory
cd ~/project/disk-file-lab

# Ensure the necessary packages are installed
sudo apt-get update
sudo apt-get install -y dd