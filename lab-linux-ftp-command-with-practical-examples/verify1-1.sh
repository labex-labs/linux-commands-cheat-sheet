#!/bin/bash
echo "FTP is a standard network protocol used for transferring files between computers over a network."
echo "FTP supports ASCII mode for text files and Binary mode for non-text files."
echo "FTP provides both Anonymous and Authenticated access methods."
exit 0