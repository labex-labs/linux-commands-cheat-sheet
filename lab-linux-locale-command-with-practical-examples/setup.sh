#!/bin/bash

# Check current locale settings
echo "Current locale settings:"
locale

# List all available locales
echo "Available locales:"
locale -a

# The script is complete, no further actions required.