#!/bin/bash

# Create the necessary directory structure
mkdir -p ~/project/info-command

# Navigate to the project directory
cd ~/project/info-command

# Ensure the necessary tools are installed
sudo apt-get update
sudo apt-get install -y info